{
  "compilerOptions": {
    /* Opções Básicas */
    "target": "es2020",                       // Especifica a versão do ECMAScript de saída
    "module": "commonjs",                     // Especifica o sistema de módulos (CommonJS para Node.js)
    "rootDir": "./src",                       // Pasta raiz onde estão os seus arquivos .ts
    "outDir": "./dist",                       // Pasta onde o JavaScript compilado será salvo
    "esModuleInterop": true,                  // Permite compatibilidade com módulos CommonJS
    "forceConsistentCasingInFileNames": true, // Garante que os nomes dos arquivos sejam consistentes

    /* Checagem de Tipos Estrita */
    "strict": true,                           // Habilita todas as opções de checagem de tipo estritas
    "skipLibCheck": true                      // Pula a verificação de tipos dos arquivos de declaração (.d.ts)
  },
  "include": ["src/**/*"],                    // Inclui todos os arquivos dentro da pasta 'src' para compilação
  "exclude": ["node_modules"]                 // Exclui a pasta node_modules
}